---
import { siteMeta, withBase } from '../lib/site';

const items = [
  ['首页', ''],
  ['关于我', '#about'],
  ['能力地图', '#capability-map'],
  ['项目作品', '#related-evidence'],
  ['经历', 'background/'],
  ['联系我', 'contact/']
];

const current = Astro.url.pathname;
const isActive = (path: string) => {
  if (path.startsWith('#')) return false;
  const href = withBase(path);
  if (!path) return current === href || current === href.replace(/\/$/, '');
  return current.startsWith(href);
};
---

<header class="site-header">
  <div class="shell header-inner">
    <a class="brand" href={withBase()} aria-label={`${siteMeta.title}首页`}>
      <span class="brand-mark" aria-hidden="true">ZYT</span>
      <span class="brand-name">{siteMeta.shortTitle}</span>
    </a>
    <button class="menu-button" type="button" aria-expanded="false" aria-controls="site-nav">
      <span class="sr-only">展开导航</span>
      <span></span><span></span>
    </button>
    <nav id="site-nav" class="site-nav" aria-label="主导航">
      {items.map(([label, path]) => (
        <a href={path.startsWith('#') ? (current === withBase() || current === withBase('').replace(/\/$/, '') ? path : withBase(path)) : withBase(path)} aria-current={isActive(path) ? 'page' : undefined}>{label}</a>
      ))}
    </nav>
  </div>
</header>

<script>
  const button = document.querySelector<HTMLButtonElement>('.menu-button');
  const nav = document.querySelector<HTMLElement>('.site-nav');
  button?.addEventListener('click', () => {
    const open = button.getAttribute('aria-expanded') === 'true';
    button.setAttribute('aria-expanded', String(!open));
    nav?.classList.toggle('is-open', !open);
  });
</script>
